LIBEFP_MAJOR?=	0
LIBEFP_MINOR?=	0

LIB=		efp
INCS=		efp.h
WARNS=		6
NO_MAN=		yes

DEBUG_FLAGS?=	-O0 -g
OMP_CFLAGS?=	-fopenmp

CFLAGS+=	${OMP_CFLAGS} \
		-I${.CURDIR}/../common \
		-DLIBEFP_VERSION="\"${LIBEFP_MAJOR}.${LIBEFP_MINOR}\""

LDADD+=		-L${.OBJDIR}/../common \
		-lcommon

SRCS=		ai_disp.c \
		ai_xr.c \
		chtr.c \
		disp.c \
		efp.c \
		elec.c \
		elec_grad.c \
		int.c \
		parse.c \
		pol.c \
		xr.c

.include <bsd.lib.mk>
