LIB=		efp
INCS=		efp.h
WARNS=		6
IGNORE_PRAGMA=	yes

VERSION!=	cat ${.CURDIR}/../VERSION

DEBUG_FLAGS?=	-O0 -g
CFLAGS+=	-DPACKAGE_VERSION="\"${VERSION}\""

SRCS=		disp.c \
		efp.c \
		elec.c \
		elec_grad.c \
		int.c \
		parse.c \
		pol.c \
		swf.c \
		xr.c

.include <bsd.lib.mk>
